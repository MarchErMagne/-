#!/bin/bash

# Скрипт для запуска миграции базы данных

echo "🔄 Применение миграции для исправления типа telegram_id..."

# Переходим в директорию проекта
cd /app || exit 1

# Применяем миграцию
alembic upgrade head

if [ $? -eq 0 ]; then
    echo "✅ Миграция успешно применена!"
    echo "🔧 Поле telegram_id теперь поддерживает большие значения (BigInteger)"
else
    echo "❌ Ошибка при применении миграции!"
    exit 1
fi

echo "🚀 Перезапустите контейнер бота для применения изменений"